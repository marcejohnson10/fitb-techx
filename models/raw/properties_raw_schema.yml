###########################################################################
# Follow is an example of how to structure a schema yaml file.
# Each folder that contains models should also contain a 
# schema yaml file describing the models contained within the folder. 
###########################################################################

version: 2

sources:
  - name: raw
    database: DBT_DEMO_PROJECT 
    schema: RAW  
    tables:
      - name: orders
#      - name: customers


#models:
#    - name: my_first_dbt_model
#      description: "A starter dbt model"
#      columns:
#          - name: id
#            description: "The primary key for this table"
#            tests:
#                - unique
#                - not_null
#
#    - name: my_second_dbt_model
#      description: "A starter dbt model"
#      columns:
#          - name: id
#            description: "The primary key for this table"
#            tests:
#                - unique
#                - not_null
